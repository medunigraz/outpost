[Unit]
Description=%I - Recording Upload Server

[Service]
Type=simple
Environment=DJANGO_SETTINGS_MODULE=outpost.django.settings
Environment=DJANGO_LOCAL_CONFIGURATION=/etc/outpost/%i.py
WorkingDirectory=/srv/outpost/%i/runtime
User=outpost
ExecSearchPath=/srv/outpost/%i/runtime/bin:/usr/bin:/bin
ExecStart=python3 -m django recordinguploadserver
UMask=0017

[Install]
WantedBy=outpost@%i.target
